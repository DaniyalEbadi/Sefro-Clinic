# راهنمای مدل‌های داده سئفر کلینیک (Sefro Clinic)

این فایل به زبان ساده رابطه بین **مشتری‌ها (Customers)**، **محصولات (Products)**، **خدمات (Services)** و **دسته‌بندی خدمات (Service Categories)** را توضیح می‌دهد.

---

## ۱. مشتری (Customer)

**فایل:** `customers/models.py` → کلاس `Customer`

مشتری کسی است که به کلینیک مراجعه می‌کند و خدمات می‌گیرد.

| فیلد | توضیح |
|-------|-------|
| `first_name` / `last_name` | نام و نام خانوادگی |
| `mobile_number` | شماره موبایل (منحصر به فرد) |
| `national_id` | کد ملی (منحصر به فرد) |
| `file_sys_id` | **کد پرونده ۴۰ کاراکتری** (جدید - برای سیستم‌های خارجی) |
| `birthday` | تاریخ تولد (شمسی) |
| `bitmoji_code` | کد بت‌موجی (اختیاری) |
| `satisfaction` | رضایت (۱ تا ۵) |
| `notes` | یادداشت‌ها |
| `created_at` | تاریخ ثبت‌نام |

**رابطه‌ها:**
- یک مشتری می‌تواند **بازدیدهای (Visits)** زیادی داشته باشد
- یک مشتری می‌تواند **پرداخت‌های (Payments)** زیادی داشته باشد

---

## ۲. دسته‌بندی خدمات (ServiceCategory)

**فایل:** `customers/models.py` → کلاس `ServiceCategory`

گروه‌بندی خدمات برای منظم‌سازی منو.

| فیلد | توضیح |
|-------|-------|
| `name` | نام دسته (مثال: "پوست"، "موی") |
| `slug` | آدرس‌های دوستانه (مثال: `skin-care`) |
| `description` | توضیحات |
| `is_active` | فعال/غیرفعال |
| `sort_order` | ترتیب نمایش |

**مثال:**
```
پوست (skin-care)
  ├─ بotox
  ├─ فیلر
  └─ لیزر

موی (hair-care)
  ├─ پرپ
  └─ ترمیم
```

---

## ۳. خدمات (Service)

**فایل:** `customers/models.py` → کلاس `Service`

خدماتی که کلینیک ارائه می‌دهد و مشتری می‌تواند رزرو کند.

| فیلد | توضیح |
|-------|-------|
| `name` | نام خدمت (مثال: "بوتاکس پیشانی") |
| `description` | توضیحات |
| `price` | قیمت نمایشی (تومان - قدیمی) |
| `price_usd` | **قیمت پایه دلاری** (استاندارد مالی) |
| `time` | مدت زمان (دقیقه) |
| `is_active` | فعال/غیرفعال |
| `category` | دسته‌بندی (ForeignKey به ServiceCategory) |

**رابطه‌ها:**
- یک خدمت متعلق به **یک دسته‌بندی** است
- یک خدمت می‌تواند در **بازدیدهای (Visits)** مختلف استفاده شود
- یک خدمت می‌تواند **محصولات (ServiceItem)** مصرف کند

---

## ۴. محصولات (Product) - انبار

**فایل:** `inventory/models.py` → کلاس `Product`

مواد مصرفی که در انجام خدمات استفاده می‌شوند.

| فیلد | توضیح |
|-------|-------|
| `name` | نام محصول (مثال: "بوتاکس ۱۰۰ واحد") |
| `unit_price` | قیمت فروش واحد |
| `cost_usd` | **هزینه واقعی دلاری** (برای محاسبه سود) |
| `count` | موجودی انبار |

**رابطه با خدمات:**
- از طریق مدل `ServiceItem` (در `finance`):
  - هر خدمت می‌تواند چند محصول مصرف کند
  - هر مصرف، مقدار مشخصی از محصول را کم می‌کند
  - این برای **محاسبه سودآوری** هر خدمت استفاده می‌شود

---

## خلاصه روابط

```
ServiceCategory (۱) ──────< (N) Service
                                      │
                                      │
Service (۱) ──────────────< (N) Visit > (N) ────── Customer
                                      │
                                      │
Service (۱) ────< (N) ServiceItem > (N) ──── Product
                                      │
                                      ▼
                              (محاسبه سود/زیان)
```

---

## API Endpoints مربوطه

| مدل | آدرس API | دسترسی |
|-----|-----------|---------|
| Customers | `/api/customers/` | Admin + Employee (CRUD کامل) |
| Service Categories | `/api/service-categories/` | Admin (نوشتن) / همه (خواندن) |
| Services | `/api/services/` | Admin + Employee |
| Products | `/api/inventory/products/` | Admin + Employee |

---

## نکات مهم

1. **قیمت‌گذاری دو لایه:** `price` (نمایشی) vs `price_usd` (استاندارد مالی)
2. **file_sys_id:** کد ۴۰ کاراکتری جدید برای یکپارچه‌سازی با سیستم‌های خارجی
3. **مجوزها:** ادمین و کارمند هر دو می‌توانند مشتری بسازند/ویرایش کنند
4. **محاسبه سود:** از طریق `ServiceItem` و `cost_usd` محصول انجام می‌شود